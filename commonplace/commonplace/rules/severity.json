{
  "_comment": "Staged severity for every modular check. fail = blocks (counts as a hard fail + blocks commit via hook). warn = review only. off = skip. Flip a check from warn->fail as its remediation phase finishes, so turning the gate on does not block every commit before the backlog is cleared. NOTE: the pe.* (Plain English) checks are NOT on this staging ramp — their fail/warn split is final by design (see _plainEnglish_status below), because the layer is already complete at 0 fails.",
  "_staging_plan": {
    "_status": "RAMP COMPLETE (as of 2026-07-01) — Phases A through E are all promoted; every modular check now sits at its final severity and there is no warn->fail backlog left to clear. Retained below as history. taxonomy.region was added directly at fail alongside the Central Asia & the Steppe canonical promotion.",
    "Phase A/B — integrity enforced (DONE)": ["ref.duplicateTitle=fail", "ref.noEssential=fail", "rabbit.blankEntryId=fail", "rabbit.dupEntryId=fail"],
    "Phase C — taxonomy enums approved (DONE)": ["taxonomy.pcType=fail", "taxonomy.theme=fail", "taxonomy.region=fail"],
    "Phase D — comparative-intro pass (DONE)": ["cm.summaryRequired=fail"],
    "Phase E — popular-culture rewrite (DONE)": ["pc.scorecard=fail"]
  },
  "_plainEnglish_status": "COMPLETE — all 1000 entries carry content.plainEnglish, corpus validates at 0 fails (as of 2026-06-30). The pe.* severities are FINAL, not staging artifacts awaiting promotion. The 7 fails ARE the transformation contract: pe.keyParity (structure mirrors Beginner), pe.sectionEmpty, pe.markdown (plain prose only), pe.punctuation (no em dash / semicolon), pe.readability (FK <= 8.0 hard), pe.sentenceMax (<= 28 words), pe.numberFidelity (every digit traces to Beginner). The 7 warns are heuristics or soft targets that MUST stay warn to avoid false-positive blocks: pe.readabilityWatch (FK <= 7 target, below the hard fail), pe.sentenceMean (<= 16 mean target), pe.properNounFidelity (name-not-in-source heuristic; false-positives on JSON/month names/etc.), pe.style + pe.styleWatch (idiom regex), pe.lengthBand (legit variance), pe.matterCap (register crutch). Spec table: PLAIN_ENGLISH_SPEC.md sec.6.",
  "severity": {
    "ref.duplicateTitle": "fail",
    "ref.likelyDuplicate": "warn",
    "ref.noEssential": "fail",
    "ref.yearFormat": "warn",
    "ref.annotationBoilerplate": "warn",
    "rabbit.blankEntryId": "fail",
    "rabbit.dupEntryId": "fail",
    "taxonomy.pcType": "fail",
    "taxonomy.theme": "fail",
    "taxonomy.region": "fail",
    "cm.summaryRequired": "fail",
    "cm.cardLength": "warn",
    "pc.scorecard": "fail",
    "pc.watch": "warn",
    "commerce.microcopy": "warn",
    "commerce.fictionCap": "fail",
    "commerce.fictionNote": "fail",
    "repetition.stem": "warn",

    "arith.dateSpan": "warn",
    "arith.age": "warn",
    "consistency.subjectYears": "warn",
    "consistency.acronym": "warn",
    "consistency.nameForms": "warn",


    "pe.keyParity": "fail",
    "pe.sectionEmpty": "fail",
    "pe.markdown": "fail",
    "pe.punctuation": "fail",
    "pe.readability": "fail",
    "pe.readabilityWatch": "warn",
    "pe.sentenceMax": "fail",
    "pe.numberFidelity": "fail",
    "pe.sentenceMean": "warn",
    "pe.properNounFidelity": "warn",
    "pe.style": "warn",
    "pe.styleWatch": "warn",
    "pe.lengthBand": "warn",
    "pe.matterCap": "warn"
  }
}
