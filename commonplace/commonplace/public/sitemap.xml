<?xml version="1.0" encoding="UTF-8"?>
<!-- The app uses client-side hash routing (#/entry/<id>), so per-entry URLs are not
     individually crawlable and are intentionally omitted. Listing /entry/<id> path URLs
     here would point crawlers at 404s. Real per-entry SEO requires path routing + a
     server rewrite + prerendering (tracked as a post-beta item). -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>https://www.thecommonplace.dev/</loc><priority>1.0</priority></url>
</urlset>
